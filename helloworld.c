#include <stdio.h>

int main(  void ) {
	printf( "welcome to study c program\n" );
	int a, b;
	printf( "pls input a and b\n" );
	scanf( "%d %d", &a, &b );
	double c = a + b;
	printf( "a = %d, b = %d, sum of a and b = %f\n", a, b, sum );
	return 0;
}
